---
'@redotlabs/ui': minor
---

Button에 `color` prop 추가. 기존 컴포넌트(Badge, Callout)와 동일한 시맨틱 색상 세트(`default | secondary | info | success | warning | danger`)를 지원하며, `contained | outlined | text` 모든 variant와 조합된다. 기본값은 `default`(primary)로 기존 동작과 동일하다.
