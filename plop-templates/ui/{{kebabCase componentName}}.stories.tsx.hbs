import { {{pascalCase componentName}} } from './{{kebabCase componentName}}';
import { Meta, StoryObj } from '@storybook/react-vite';

const meta: Meta<typeof {{pascalCase componentName}}> = {
  title: 'Components/{{pascalCase componentName}}',
  component: {{pascalCase componentName}},
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component: 'Redot {{description}} Component',
      },
    },
  },
  tags: ['autodocs'],
  argTypes: {
    variant: {
      control: {
        type: 'inline-radio',
      },
      defaultValue: 'default',
      description: 'The variant of the {{pascalCase componentName}}',
      table: {
        type: { summary: 'default' },
        defaultValue: { summary: 'default' },
      },
      options: ['default'],
    },
    size: {
      control: {
        type: 'inline-radio',
      },
      defaultValue: 'md',
      table: {
        type: { summary: 'md' },
        defaultValue: { summary: 'md' },
      },
      description: 'The size of the {{pascalCase componentName}}',
      options: ['md'],
    },
    children: {
      control: {
        type: 'text',
      },
      description: 'The content of the {{pascalCase componentName}}',
    },
  },
};

export default meta;
type Story = StoryObj<typeof {{pascalCase componentName}}>;

export const Default: Story = {
  args: {
    children: '{{pascalCase componentName}} Component',
    variant: 'default',
    size: 'md',
  },
};

export const Variants: Story = {
  args: {
    children: '{{pascalCase componentName}} Component',
    variant: 'default',
    size: 'md',
  },
  render: (args) => (
    <div className="flex items-center gap-4">
      <{{pascalCase componentName}} {...args} variant="default" />
    </div>
  ),
};

export const Sizes: Story = {
  args: {
    children: '{{pascalCase componentName}} Component',
    variant: 'default',
    size: 'md',
  },
  render: (args) => (
    <div className="flex items-center gap-4">
      <{{pascalCase componentName}} {...args} size="md" />
    </div>
  ),
};